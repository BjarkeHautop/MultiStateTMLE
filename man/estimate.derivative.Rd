% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.derivative.R
\name{estimate.derivative}
\alias{estimate.derivative}
\title{Estimate the numerical derivative of an alpha-indexed estimand}
\usage{
estimate.derivative(
  alpha_hat,
  fun = alpha.est.fun,
  parameter = "z",
  h = length(unique(dt[, id]))^{
     -1/6
 },
  ...
)
}
\arguments{
\item{alpha_hat}{numeric; the point at which to estimate the derivative.}

\item{fun}{function taking \code{alpha}, \code{parameter}, and \code{...}, returning a
list with an \code{estimate} element containing \code{tmle.est}/\code{one.step.est}.}

\item{parameter}{character; the target process/outcome passed to \code{fun}.}

\item{h}{numeric step size for the finite difference; shrunk if it exceeds
\code{alpha_hat}.}

\item{...}{additional arguments passed on to \code{fun}.}
}
\value{
numeric; the estimated derivative.
}
\description{
Approximates the derivative with respect to \code{alpha} of the TMLE/one-step
estimate returned by \code{fun}, using a symmetric finite-difference at
\code{alpha_hat}.
}
