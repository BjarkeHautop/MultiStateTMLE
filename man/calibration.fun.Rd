% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.fun.R
\name{calibration.fun}
\alias{calibration.fun}
\title{Calibrate a shape-parameter intervention to a target effect size}
\usage{
calibration.fun(
  initial.fit = NULL,
  a = NULL,
  theta = 0.5,
  rho = NULL,
  delta = NULL,
  rho.1a = NULL,
  browse = FALSE,
  verbose = TRUE,
  output.eic = FALSE,
  tau = 1.2,
  tau.z = tau,
  use.cores = 1,
  target = "outcome",
  z.name = "z",
  ...
)
}
\arguments{
\item{initial.fit}{initial fit object, as returned by \code{\link[=prepare.initial]{prepare.initial()}}.}

\item{a}{optional intervention on the baseline treatment \code{A0}, passed on to
\code{\link[=tmle.alpha.fun]{tmle.alpha.fun()}}.}

\item{theta}{optional numeric; target value for the estimand to calibrate
to directly.}

\item{rho}{optional numeric; target fraction of the \code{alpha = 1} effect on
\code{target} to calibrate to.}

\item{delta}{optional numeric; target absolute difference from the
\code{alpha = 1} effect on \code{target} to calibrate to.}

\item{rho.1a}{optional numeric; as \code{rho}, but relative to the effect under
\code{a} fixed to 1 rather than \code{alpha = 1}.}

\item{browse}{logical; if \code{TRUE}, drop into \code{browser()}.}

\item{verbose}{logical; if \code{TRUE}, print progress.}

\item{output.eic}{logical; if \code{TRUE}, include influence-function values in
the output.}

\item{tau}{follow-up horizon for the \code{target} process.}

\item{tau.z}{follow-up horizon for the \code{z} process (defaults to \code{tau}).}

\item{use.cores}{number of cores to use.}

\item{target}{character; name of the outcome process being calibrated to.}

\item{z.name}{character; name of the process on which the shape
intervention \code{alpha} acts.}

\item{...}{additional arguments passed on to \code{\link[=tmle.alpha.fun]{tmle.alpha.fun()}}.}
}
\value{
A list with the calibrated \code{alpha} (as found by
\code{\link[=estimate.alpha.fun]{estimate.alpha.fun()}}) and the corresponding TMLE fit(s).
}
\description{
Finds the intensity-shape intervention \code{alpha} (via \code{\link[=estimate.alpha.fun]{estimate.alpha.fun()}}
and \code{\link[=tmle.alpha.fun]{tmle.alpha.fun()}}) for the \code{z} process such that the resulting effect
on \code{target} matches a prespecified fraction \code{rho} of the effect under
\code{alpha = 1}, or a fixed \code{theta}/\code{delta}.
}
\examples{
set.seed(1405)
baseline <- list(L0 = function(N) rnorm(N))
processes <- list(
  z = list(type = "one.jump", eta = 0.2, nu = 1),
  outcome1 = list(type = "terminal", eta = 0.3, nu = 1),
  censoring = list(type = "censoring", eta = 0.1, nu = 1)
)
effects <- list(
  c("L0", "z", 0.5),
  c("z", "outcome1", 0.7)
)
dt <- sim.generic(baseline = baseline, processes = processes, effects = effects, n = 30)

initial.fit <- prepare.initial(
  dt,
  tau = 1,
  fit.types = list(
    z = list(
      model = "Surv(tstart, tstop, delta == 2)~L0",
      fit = "cox",
      at.risk = function(dt) (dt[["z"]] == 0)
    ),
    outcome1 = list(model = "Surv(tstart, tstop, delta == 1)~L0+z", fit = "cox"),
    censoring = list(model = "Surv(tstart, tstop, delta == 0)~L0", fit = "cox")
  ),
  verbose = FALSE
)

# Calibrate alpha so the risk of outcome1 by tau = 1 equals 0.2.
cal <- calibration.fun(
  initial.fit = initial.fit,
  theta = 0.2,
  tau = 1,
  target = "outcome1",
  z.name = "z",
  use.cores = 1,
  verbose = FALSE
)
cal$estimate
}
