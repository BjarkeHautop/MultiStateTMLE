% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.alpha.fun.R
\name{estimate.alpha.fun}
\alias{estimate.alpha.fun}
\title{Search for the alpha achieving a target estimand value}
\usage{
estimate.alpha.fun(
  theta,
  fun,
  c_n,
  alpha_init = 1,
  expand_up = 2,
  expand_down = 0.25,
  max_iter = 100,
  alpha_min = 0.001,
  alpha_max = 100,
  use.cores = 1,
  verbose = FALSE,
  trace_every = 1L
)
}
\arguments{
\item{theta}{numeric; the target value of the estimand to solve for.}

\item{fun}{function of \code{alpha} (plus \code{...}) returning a list with an
\code{estimate} element and an \code{eic} element, e.g. \code{\link[=tmle.alpha.fun]{tmle.alpha.fun()}}.}

\item{c_n}{numeric; a finite tolerance/scale constant used to judge
convergence (required, no default).}

\item{alpha_init}{numeric; initial value of \code{alpha} to evaluate.}

\item{expand_up}{multiplicative factor used to expand the bracket upward.}

\item{expand_down}{multiplicative factor used to expand the bracket
downward.}

\item{max_iter}{maximum number of root-finding iterations.}

\item{alpha_min}{lower bound for \code{alpha} (must be \verb{> 0}).}

\item{alpha_max}{upper bound for \code{alpha} (must be \verb{> alpha_min}).}

\item{use.cores}{number of cores to pass on to \code{fun}.}

\item{verbose}{logical; if \code{TRUE}, print progress at every \code{trace_every}
evaluations.}

\item{trace_every}{integer; frequency (in evaluations) of progress output
when \code{verbose = TRUE}.}
}
\value{
A list describing the solution, including the selected \code{alpha} and
the corresponding evaluation of \code{fun}.
}
\description{
Searches over the shape-parameter intervention \code{alpha} for the value at
which \code{fun(alpha, ...)}'s estimate equals \code{theta}, via bracket expansion
followed by root finding, caching evaluations of \code{fun} along the way.
}
\examples{
# Psi_z^alpha(P) = 0.6 * (1 - exp(-alpha)), with a known inverse,
# used here to check that estimate.alpha.fun recovers it numerically.
# eic mimics a real per-subject efficient influence curve, as returned
# by tmle.alpha.fun().
set.seed(1405)
fun <- function(alpha) {
  psi <- 0.6 * (1 - exp(-alpha))
  n <- 200
  ic <- rnorm(n, sd = 0.05)
  ic <- ic - mean(ic)
  list(estimate = c(tmle.est = psi, se = sd(ic) / sqrt(n)), eic = ic)
}

res <- estimate.alpha.fun(theta = 0.3, fun = fun, c_n = 1e-4, alpha_init = 1)
res$alpha.hat
res$converged
}
