% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.from.data.R
\name{sim.from.data}
\alias{sim.from.data}
\title{Simulate event history data from parameters fitted to observed data}
\usage{
sim.from.data(
  n = 500,
  sim.parameters,
  cens = 1,
  alpha.intervention = list(),
  baseline.intervention = list(),
  browse = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{n}{number of individuals to simulate.}

\item{sim.parameters}{list of fitted simulation parameters, with one entry
per process (Weibull \code{weibull.parameters} and \code{cox.parameters}), a
\code{baseline.summary} entry describing baseline covariate distributions, and
a \code{model.structure} entry describing process names/types/order.}

\item{cens}{numeric at-risk indicator scaling for the censoring process.}

\item{alpha.intervention}{named list of multiplicative interventions on
process intensities (\code{eta}), keyed by process name.}

\item{baseline.intervention}{named list of interventions that fix a
baseline covariate to a constant value.}

\item{browse}{logical; if \code{TRUE}, drop into \code{browser()} before simulating.}

\item{verbose}{logical; if \code{TRUE}, print the resolved \code{eta}/\code{nu}/\code{beta}
parameters.}
}
\value{
A \code{data.table} of simulated event history data with columns \code{id},
\code{time}, \code{delta}, baseline covariates, and one column per process.
}
\description{
Simulates new multistate event history data using process/baseline
parameters previously estimated from observed data (e.g. via
\code{\link[simevent:simEventCox]{simevent::simEventCox()}}), via \code{\link[simevent:simEventData]{simevent::simEventData()}}.
}
