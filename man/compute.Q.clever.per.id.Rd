% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.Q.clever.per.id.R
\name{compute.Q.clever.per.id}
\alias{compute.Q.clever.per.id}
\title{Compute Q and clever covariates per id using a discrete-state backward recursion}
\usage{
compute.Q.clever.per.id(
  dt_id,
  states,
  process.types = NULL,
  P.prefix = "P.",
  state.idx.col = "state",
  parameter = "target",
  process.deltas = NULL,
  compute.clever = TRUE,
  get.years.lost = FALSE,
  years.lost.block.size = 10,
  clever.by.state = FALSE
)
}
\arguments{
\item{dt_id}{data.table with time-ordered rows for a single id. Must contain columns for hazards of the form P.\if{html}{\out{<name>}}.\if{html}{\out{<s>}}.}

\item{states}{data.table enumerating the product state space. Must have S rows and columns for each stateful process (named by process) plus a column \code{state} giving indices 1..S.}

\item{process.types}{named character vector or list describing each discovered process type. Allowed values include: "terminal", "recurrent", "one.jump", "state-with-atrisk". If omitted, a small heuristic is used.}

\item{P.prefix}{prefix used for hazard columns in dt_id (default "P."). The function looks for columns matching \verb{^P.prefix<name>.<s>$}.}

\item{state.idx.col}{name of the column in dt_id that holds the state index (value in 1..S). Default "state".}

\item{parameter}{character: name of the target process/outcome. If "target" (default), the first discovered terminal/outcome name is used.}

\item{process.deltas}{optional numeric vector (length = number of discovered processes). Not used internally for mapping, only validated if provided.}

\item{compute.clever}{logical; whether to compute and append clever.Q.\if{html}{\out{<name>}}0 / clever.Q.\if{html}{\out{<name>}}1 (default TRUE).}

\item{get.years.lost}{logical; whether to also compute a years-lost-type summary.}

\item{years.lost.block.size}{block size used when computing the years-lost summary (when \code{get.years.lost = TRUE}).}

\item{clever.by.state}{logical; whether to compute clever covariates by state rather than pooled.}
}
\value{
data.table: \code{dt_id} augmented with column \code{Q} and columns \verb{clever.Q.<name>0} and \verb{clever.Q.<name>1} for each discovered name.
}
\description{
The function expects a time-ordered \code{dt_id} (rows for a single id) that
contains hazard columns named like \verb{P.<name>.<s>} for each discovered
process \verb{<name>} and state index \code{s} in \verb{1..S}. It uses the product-space
\code{states} table to map state indices and to build successor mappings.
}
\examples{
# S = 1, single terminal process "D": Q_t collapses to the discrete-time
# cumulative incidence 1 - prod(1 - h_s).
h <- c(0.1, 0.2, 0.3)
dt_id <- data.table::data.table(
  state = c(1L, 1L, 1L),
  state.row.index = c(1L, 1L, 1L),
  P.D.1 = h
)
states <- data.table::data.table(state = 1L)

compute.Q.clever.per.id(
  dt_id = dt_id,
  states = states,
  process.types = list(D = "terminal")
)
}
