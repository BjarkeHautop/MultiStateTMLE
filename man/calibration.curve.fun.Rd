% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.curve.fun.R
\name{calibration.curve.fun}
\alias{calibration.curve.fun}
\title{Compute a calibration curve over a grid of shape-parameter interventions}
\usage{
calibration.curve.fun(
  initial.fit = NULL,
  a = NULL,
  alpha.grid = seq(0, 5, length = 10),
  browse = FALSE,
  verbose = TRUE,
  output.eic = FALSE,
  tau = 1.2,
  tau.z = tau,
  use.cores = 50,
  target = "outcome",
  z.name = "z",
  min.iter = 1,
  target.by.state = FALSE,
  block.size.z = NULL,
  block.size.target = NULL,
  ...
)
}
\arguments{
\item{initial.fit}{initial fit object, as returned by \code{\link[=prepare.initial]{prepare.initial()}}.}

\item{a}{optional intervention on the baseline treatment \code{A0}, passed on to
\code{\link[=tmle.alpha.fun]{tmle.alpha.fun()}}.}

\item{alpha.grid}{numeric vector of shape-parameter interventions to
evaluate.}

\item{browse}{logical; if \code{TRUE}, drop into \code{browser()}.}

\item{verbose}{logical; if \code{TRUE}, print progress.}

\item{output.eic}{logical; if \code{TRUE}, include influence-function values in
the output.}

\item{tau}{follow-up horizon for the \code{target} process.}

\item{tau.z}{follow-up horizon for the \code{z} process (defaults to \code{tau}).}

\item{use.cores}{number of cores to use.}

\item{target}{character; name of the outcome process the curve is
computed for.}

\item{z.name}{character; name of the process on which the shape
intervention \code{alpha} acts.}

\item{min.iter}{minimum number of TMLE update iterations.}

\item{target.by.state}{logical; whether to compute clever covariates by
state, passed on to \code{\link[=tmle.alpha.fun]{tmle.alpha.fun()}}.}

\item{block.size.z}{optional block size used when evaluating the \code{z}
process.}

\item{block.size.target}{optional block size used when evaluating
\code{target}.}

\item{...}{additional arguments passed on to \code{\link[=tmle.alpha.fun]{tmle.alpha.fun()}}.}
}
\value{
A \code{data.table} with one row per value in \code{alpha.grid}, giving the
corresponding TMLE estimate of the effect on \code{target}.
}
\description{
Evaluates \code{\link[=tmle.alpha.fun]{tmle.alpha.fun()}} on the \code{z} process across a grid of shape
interventions \code{alpha.grid}, and the resulting effect on \code{target} for each,
tracing out a calibration curve relating \code{alpha} to the target estimand.
}
