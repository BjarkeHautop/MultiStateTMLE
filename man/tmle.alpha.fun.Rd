% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmle.alpha.fun.R
\name{tmle.alpha.fun}
\alias{tmle.alpha.fun}
\title{Targeted maximum likelihood estimation under a shape-parameter intervention}
\usage{
tmle.alpha.fun(
  target = "z",
  tau = 1.2,
  alpha = 1,
  z.name = "z",
  alpha.list = NULL,
  a = NULL,
  initial.fit = NULL,
  dt = NULL,
  years.lost = NULL,
  only.first = NULL,
  target.only.in.state = NULL,
  target.by.state = FALSE,
  conv.const = 1,
  one.step = FALSE,
  verbose = FALSE,
  max.iter = 10,
  min.iter = 0,
  use.cores = 50,
  truncate.weights = 0,
  output.convergence = FALSE,
  output.eic = FALSE,
  output.weights = NULL,
  output.a.weights = NULL,
  browse = FALSE,
  verbose.exponential = FALSE,
  ...
)
}
\arguments{
\item{target}{character; name of the target process/outcome.}

\item{tau}{follow-up horizon (in time units) for \code{target}.}

\item{alpha}{numeric; multiplicative shape-parameter intervention applied
to the \code{z} process (ignored if \code{alpha.list} is supplied).}

\item{z.name}{character; name of the process on which \code{alpha} acts.}

\item{alpha.list}{optional named list of intervention functions, one per
process, of the form \verb{function(time, covariates)}; supersedes \code{alpha}.}

\item{a}{optional intervention on the baseline treatment \code{A0} (must match
the \code{a} used to build \code{initial.fit}, if \code{initial.fit} is supplied).}

\item{initial.fit}{initial fit object, as returned by \code{\link[=prepare.initial]{prepare.initial()}};
computed from \code{dt}/\code{tau}/\code{a}/\code{...} if not supplied.}

\item{dt}{data.table of observed event history data, passed to
\code{\link[=prepare.initial]{prepare.initial()}} when \code{initial.fit} is not supplied.}

\item{years.lost}{optional; if supplied, computes years-lost-type
estimands with this block size.}

\item{only.first}{logical; if \code{TRUE}, restrict \code{target} to its first
occurrence (treat it as a one-jump process).}

\item{target.only.in.state}{optional function of \code{states} identifying the
subset of states in which \code{target} events are counted.}

\item{target.by.state}{logical; whether to compute clever covariates by
state rather than pooled.}

\item{conv.const}{constant scaling the convergence criterion on the mean
efficient influence curve.}

\item{one.step}{logical; if \code{TRUE}, return a one-step estimator instead of
iterating to TMLE convergence.}

\item{verbose}{logical; if \code{TRUE}, print iteration progress.}

\item{max.iter}{maximum number of TMLE update iterations.}

\item{min.iter}{minimum number of TMLE update iterations before checking
convergence.}

\item{use.cores}{number of cores to use for the per-id computations.}

\item{truncate.weights}{numeric; if \verb{> 0}, truncate clever weights above
this value.}

\item{output.convergence}{logical; if \code{TRUE}, include convergence
diagnostics in the output.}

\item{output.eic}{logical; if \code{TRUE}, include the efficient influence
curve in the output.}

\item{output.weights}{optional numeric vector of quantiles at which to
summarize the clever/censoring weights.}

\item{output.a.weights}{optional numeric vector of quantiles at which to
summarize the \code{a}-specific clever weights.}

\item{browse}{logical; if \code{TRUE}, drop into \code{browser()}.}

\item{verbose.exponential}{logical; if \code{TRUE}, print diagnostics from the
exponential TMLE update step.}

\item{...}{additional arguments passed on to \code{\link[=prepare.initial]{prepare.initial()}} when
\code{initial.fit} is not supplied.}
}
\value{
A list with the TMLE \code{estimate}, standard error, and (depending on
the arguments above) the efficient influence curve, convergence
diagnostics, and weight summaries.
}
\description{
Estimates the effect of a stochastic intensity intervention (scaling the
shape of the \code{z} process by \code{alpha}, optionally combined with an
intervention \code{a} on baseline treatment) on \code{target}, via an iterative TMLE
update of the initial fit produced by \code{\link[=prepare.initial]{prepare.initial()}}.
}
